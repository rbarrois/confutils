ConfUtils
=========

The ConfUtils package provides enhanced config file handling tools, focused on
writing a end-user program instead of the auto-generation approach of Python's
``configparser``.

It is compatible with all versions of Python from 2.6 to 3.X.
